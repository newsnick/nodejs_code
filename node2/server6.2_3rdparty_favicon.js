// The /favicon.ico request is automatically generated by web browsers
// when loading a webpage. It is used to request the website's favicon,
// which is a small icon that represents the website and is typically displayed in the browser's tab.

// const http = require('http');
// const server = http.createServer((request, response) => {
//   const url = request.url;
//   response.write('Hello, Node JS!\n');
//   console.log(url);
//   // Sending the response
//   response.end();
// });

// server.listen(8080, () => {
//   console.log('Server is listening on port 8080');
// });
// To avoid the request for favicon in your Node.js code,A condition to check is added.
const http = require('http')

const server = http.createServer((request, response) => {
  const url = request.url
  if (url !== '/favicon.ico') {
    response.write('Hello, Node JS!\n')
    console.log(url)
  }
  response.end()
})

server.listen(8080, () => {
  console.log('Server is listening on port 8080')
})
